# Nginx Anti-DDoS Global Rate Limiting Zone Configuration
# Include this in /etc/nginx/conf.d/anti-ddos-limits.conf or inside http {} block in nginx.conf

limit_req_zone $binary_remote_addr zone=ddos_limit:20m rate=30r/s;
limit_conn_zone $binary_remote_addr zone=ddos_conn:20m;
